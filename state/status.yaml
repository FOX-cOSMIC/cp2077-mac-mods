---
project: cp2077-mac-mods
version: 0.1.0
last_updated: 2026-07-02
current_phase:
  id: 1
  name: foundation-engineering
  progress_pct: 85
  description: 'MILESTONE (F-044): first VISIBLE, VERIFIED, native-macOS in-game change
    achieved — a direct live-memory write changed the player''s on-screen eddies
    310,915 -> 400 on an existing save, no reload, no crash. Mechanism is the
    LIVE-WRITE path (mach_vm_read_overwrite/mach_vm_write + structural/value scans via
    DoStatProbeCells, TWEAKXL_POKE_PROBE_CELLS=1), NOT the TweakDB-flat path (which
    remains walled per F-043/FA-016). Key model finding: derived/displayed stats
    (RAM cap 32, max HP 322) are computed from base+modifiers and NEVER materialized as
    their displayed value, so they are unreachable by value-scan (FA-017/18/19); STORED
    counters (money/components/XP) exist verbatim in memory and ARE writable+visible.
    The full TweakDB WRITE/CREATE/APPEND stack stays proven+verified at the engine''s
    own accessors (F-036/F-041/F-042) but does not propagate to gameplay on macOS.'
phase_0_summary:
  closed_at: 2026-05-29
  duration_days: 1
  facts_validated: 19
  failed_approaches_documented: 11
  hypotheses_resolved_fact: 4
  hypotheses_resolved_failed: 2
  hypotheses_still_open: 3
  key_discoveries:
  - TweakDB struct = 0x168 bytes (same as Windows, NOT 0x198)
  - No vtable, no fn-ptr dispatch — pure data layout
  - Singleton at 0x1080c92d0, slide-adjusted
  - Apply-trigger at FUN_102b75744 — single-caller orchestrator
  - Re-signing NOT required for v1.0 (stock 2.3.1 has needed entitlements)
  - psiberx .tweak parser reusable via TWEAKXL_MAC_OFFLINE flag
blockers:
- 'F-038/FA-012: no flat gameInstance root on macOS -> the TweakDB-driven live stat
  oracle / propagation path stays blocked. Does NOT block visible change: F-044 reached
  live gameplay state by a structural heap-scan + direct write, no gameInstance needed.'
- 'TweakDB-flat -> gameplay propagation wall (F-043/FA-013..FA-016): flat/record edits
  verify at GetFlat/GetRecord but do not manifest in gameplay. Open; visible change was
  achieved by the independent live-write path instead.'
pending_decisions: []   # direction RESOLVED by D-007 (2026-06-10): sequence in docs/ROADMAP.md
next_action:
  agent: lead
  task_id: PHASE2-CONTINUE
  description: 'Phase 2 (docs/PHASE_2_PLAN.md) in progress. DONE: P2.1 set-to-value (commit
    2171625, in-game validated); P2.5 version/offset gate (Mach-O LC_UUID fingerprint vs
    validated 2.3.1 build A6656ADC-...-645089; mismatch disables writes; verified in-game OK);
    P2.6 user guide docs/guides/live-edit.md + unit tests (LiveEditParse.hpp helpers extracted;
    liveedit_parse_test; fixed root enable_testing() ordering so ctest registers subdir tests —
    3 tests, 100% pass); P2.2 delta-narrow targeting (commit 3e59527 — find/narrow/setcand for
    common/small-valued counters; validated in-game money 310915->narrow->1,000,000->revert;
    adversarial review caught+fixed 1 critical revert bug). Phase 2 CORE COMPLETE: live-edit
    tool is usable (set + delta-narrow + revert), safe (guards/caps/version-gate/dedup),
    documented (docs/guides/live-edit.md), tested (ctest). Launcher /tmp/run_liveedit.sh.
    PHASE 3 TESTED (F-045): native .archive mod loading is NOT wired on macOS (lsof + fs_usage
    both confirm no mod folder scanned) -> textures/assets now depend on Phase 5 hooking; Phase 3
    moves behind Phase 5. DISCOVERY (F-046): the install ships a NATIVE redscript compiler
    (engine/tools/scc + launch_modded.sh) + InputLoader -> redscript/.reds mods likely already
    work; it is the cheapest UNVERIFIED win. REDSCRIPT VERIFIED, NEGATIVE (F-047, 2026-07-02):
    redscript compiles cleanly on macOS (confirms F-046''s compile half; scc -> final.redscripts,
    exit 0, two independent launches) but the game NEVER loads r6/cache/final.redscripts at
    runtime — confirmed via lsof (single-shot + rapid polling across two full
    startup->menu->save-load windows), zero opens of any redscript-related path. Binary strings
    show RedScriptsHost/final.redscripts exist in compiled code but the load path is not reached
    on macOS. This invalidates F-046''s "redscript is the cheapest unverified win / no Phase-5
    hook needed" framing: redscript now needs the same category of fix as Phase 3 (F-045) and
    moves alongside it in the roadmap, likely clustering around Phase 5 hooking work (see ROADMAP
    Phase 6 re-sequencing note, 2026-07-02). OPEN (decision pending with user): (a) RE work to
    find why RedScriptsHost''s native load path isn''t reached on macOS (could be a cheaper
    unlock than full Phase 5 hooking) vs (b) proceed to Phase 4 TweakDB->gameplay parity RE vs
    (c) start Phase 5 hooking foundation directly.'
recent_facts_added:
- F-036
- F-037
- F-038
- F-039
- F-040
- F-041
- F-042
- F-043
- F-044
- F-046
- F-047
recent_failed_added:
- FA-012
- FA-013
- FA-014
- FA-015
- FA-016
- FA-017
- FA-018
- FA-019
- FA-020
long_term_vision: 'Every gameplay-changing Windows Cyberpunk 2077 mod works on macOS
  unchanged. Full ecosystem: TweakXL, ArchiveXL, CET, Codeware, redscript.

'
version_roadmap:
  v1.0:
    name: Foundation
    scope: RED4ext-mac framework + TweakXL working
    status: in-progress (current focus)
  v1.x:
    name: Assets
    scope: ArchiveXL parity (custom archives, mesh/texture/sound replacement)
    status: planned
  v2.0:
    name: Scripting
    scope: CET (Lua + ImGui-on-Metal), redscript runtime, Codeware
    status: planned
v1_progress:
  red4ext_loads_into_game: 1
  hooks_work: 1            # functional equiv: apply-trigger polling (inline hooks blocked, FA-001)
  tweakxl_plugin_loads: 1
  yaml_mods_parse: 1
  mods_apply_ingame: 0     # TweakDB yaml-mod edits APPLY + verify via game GetFlat (F-036) but
                           # do NOT visibly propagate to gameplay (F-043/FA-016). NOTE: a VISIBLE
                           # in-game change WAS achieved via the live-memory-write path (F-044,
                           # money), a different mechanism than a .yaml TweakDB mod.
  five_nexus_mods_validated: 0
  one_hour_stability: 0
  user_install_guide: 0
