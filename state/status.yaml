---
project: cp2077-mac-mods
version: 0.1.0
last_updated: 2026-06-02
current_phase:
  id: 1
  name: foundation-engineering
  progress_pct: 75
  description: 'Phase 1 framework PROVEN end-to-end. Inject -> slide -> singleton ->
    apply-trigger poll -> flat resolve(+0x40) -> interning-safe edit(+0x148 buffer) ->
    UpdateRecord(factory+Assign) all work in-vivo, and the edit is GROUND-TRUTH verified
    via the game''s own GetFlat (F-036). Two dylibs build clean. Current focus: produce
    a VISIBLE, verified in-game change. Key finding F-039: player base HP/RAM are NOT
    stored flats -- they are COMPUTED per-entity at stat-init from attributes/level
    curves (live state, behind the F-038 no-gameInstance wall), so they cannot be
    doubled by a scalar-flat edit. Pivot: target a STORED, editable, on-screen
    Memory(RAM) ConstantStatModifier.value on a RAM-granting cyberware
    (Items.*MemoryBoost*_inlineN, nameable + CRC-verified). Built IdentifyStatModifiers
    (env TWEAKXL_ID_MEM_MODS=1) to find which carry statType=Memory. Gibbed TweakDB
    schema cloned to reference/gibbed-schema/ (gitignored). Honest status: flat writes
    apply + verify at the flat level; a VISIBLE gameplay effect is not yet demonstrated.

'
phase_0_summary:
  closed_at: 2026-05-29
  duration_days: 1
  facts_validated: 19
  failed_approaches_documented: 11
  hypotheses_resolved_fact: 4
  hypotheses_resolved_failed: 2
  hypotheses_still_open: 3
  key_discoveries:
  - TweakDB struct = 0x168 bytes (same as Windows, NOT 0x198)
  - No vtable, no fn-ptr dispatch — pure data layout
  - Singleton at 0x1080c92d0, slide-adjusted
  - Apply-trigger at FUN_102b75744 — single-caller orchestrator
  - Re-signing NOT required for v1.0 (stock 2.3.1 has needed entitlements)
  - psiberx .tweak parser reusable via TWEAKXL_MAC_OFFLINE flag
blockers:
- 'F-038/FA-012: no flat gameInstance root on macOS -> live per-entity stat read-back
  (oracle) is blocked. Does NOT block the mod EFFECT (apply at TweakDB-init seeds the
  StatsContainer), only automated on-screen verification.'
- 'F-039: player base HP/RAM are computed, not stored -> cannot be doubled via a scalar
  flat. Pivot to a stored Memory(RAM) ConstantStatModifier (in progress).'
pending_decisions: []
next_action:
  agent: lead
  task_id: RAM-ID
  description: 'Run the IdentifyStatModifiers probe at the title screen
    (TWEAKXL_ID_MEM_MODS=1, helper /tmp/run_id_memmods.sh) and grep "[mem-mod] MATCH"
    in /tmp/red4ext-mac.log to find which Items.*MemoryBoost*_inlineN records carry a
    stored statType=Memory ConstantStatModifier.value. Then re-run with
    TWEAKXL_DOUBLE_STATS=1 to 2x it, confirm via the game GetFlat, and load a save with
    that cyberware equipped to see the RAM bar grow. Fallback if no editable Memory
    value: item price (live-read, on-screen via vendor). Plan:
    ~/.claude/plans/cached-snuggling-kay.md.

'
recent_facts_added:
- F-029
- F-030
- F-031
- F-032
- F-033
- F-034
- F-035
- F-036
- F-037
- F-038
- F-039
recent_failed_added:
- FA-001
- FA-002
- FA-003
- FA-004
- FA-005
- FA-006
- FA-007
- FA-008
- FA-009
long_term_vision: 'Every gameplay-changing Windows Cyberpunk 2077 mod works on macOS
  unchanged. Full ecosystem: TweakXL, ArchiveXL, CET, Codeware, redscript.

'
version_roadmap:
  v1.0:
    name: Foundation
    scope: RED4ext-mac framework + TweakXL working
    status: in-progress (current focus)
  v1.x:
    name: Assets
    scope: ArchiveXL parity (custom archives, mesh/texture/sound replacement)
    status: planned
  v2.0:
    name: Scripting
    scope: CET (Lua + ImGui-on-Metal), redscript runtime, Codeware
    status: planned
v1_progress:
  red4ext_loads_into_game: 1
  hooks_work: 1            # functional equiv: apply-trigger polling (inline hooks blocked, FA-001)
  tweakxl_plugin_loads: 1
  yaml_mods_parse: 1
  mods_apply_ingame: 0     # flat write APPLIES + verified via game GetFlat (F-036), but a
                           # VISIBLE gameplay effect is not yet demonstrated (in progress: RAM)
  five_nexus_mods_validated: 0
  one_hour_stability: 0
  user_install_guide: 0
